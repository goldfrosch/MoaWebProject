import React from "react";
import styled from "styled-components";

interface ButtonProps {
  btnCmd?: () => void;
}
const Button: React.FC<ButtonProps> = ({ btnCmd }) => {
  return <></>;
};

type ButtonBlockProps = {};
const ButtonBlock = styled.div``;
